export class Debug {
  public static pathEnabled: boolean = false;
}


/*level.ts
    let length = this.previousTwoSections.length;
    if (length >= 2) {
      if (this.previousTwoSections[length - 1].type == Type.Space) {
        nextType = this.previousTwoSections[length - 2].type;
      }
    }
*/
